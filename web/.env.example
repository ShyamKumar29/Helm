# Copy to .env.local for a non-default backend location. The default already points at a
# locally-running `uvicorn api.main:app` (api/main.py mounts every route under /api), so most
# setups don't need this file at all.
VITE_API_BASE=http://localhost:8000/api
